// ============================================================
//  SUPABASE CONFIG  — fill these in after creating your project
//  (see README.md, step 2). These are SAFE to expose publicly:
//  the anon key only allows what your table's Row Level Security
//  policies permit (insert + read wishes, nothing else).
// ============================================================
window.GETWELL_CONFIG = {
  SUPABASE_URL: "https://YOUR-PROJECT.supabase.co",
  SUPABASE_ANON_KEY: "YOUR-ANON-PUBLIC-KEY",
};
